import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'Hello MEAN 2.0!';
  description = 'A quickstarter for running Angular 2 using NodeJs + Express as the backend server and MongoDB database';
}
